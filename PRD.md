# PRD — 착한코드검거단

## 1. 프로젝트 개요

착한코드검거단은 Python `.py` 파일을 업로드하면 해당 코드 안에서 보안적으로 잘 작성된 부분과 개선이 필요한 부분을 찾아주는 AI 보안 코딩 검증 서비스다.

악성코드검거단이 나쁜 코드를 찾아내는 서비스라면, 착한코드검거단은 다음 두 가지를 함께 한다.

1. 위험한 보안 코드를 찾아서 알린다.
2. 보안적으로 잘 작성된 코드를 찾아서 “착한코드”로 칭찬한다.

핵심 메시지는 “문제가 없다고 말하는 도구”가 아니라 **보안에 좋은 증거를 보여주고, 개선 방향을 알려주는 도구**다.

## 2. 해결하려는 문제

기존 코드 분석 결과는 위험 경고만 많이 보여주기 때문에 초보 개발자는 무엇을 잘하고 있는지 알기 어렵다. 또한 AI에게 코드를 분석해달라고만 하면 근거 없는 판단이나 과장된 보안 보장이 발생할 수 있다.

따라서 이 프로젝트는 다음 구조를 사용한다.

`기계적으로 재현 가능한 규칙 분석 → 근거가 포함된 결과 → AI의 이해하기 쉬운 설명 → 사용자의 수정 → 재검사`

## 3. 대상 사용자

- 보안 코딩을 배우는 학생
- Python 과제나 프로젝트를 검사하는 개발자
- 코드 리뷰에서 보안 습관을 빠르게 확인하는 팀

## 4. 사용자 흐름

1. 사용자가 `.py` 파일을 선택한다.
2. 확장자, 파일 크기, 인코딩을 검사한다.
3. 파일을 실행하지 않고 Python AST 기반 정적 분석을 한다.
4. 위험 규칙과 착한코드 규칙을 각각 실행한다.
5. 각 결과에 규칙 ID, 심각도, 신뢰도, 줄 번호, 근거를 붙인다.
6. AI는 분석 결과를 바탕으로 요약·교육용 설명·우선순위를 만든다.
7. 사용자는 수정 후 같은 파일을 다시 검사한다.

## 5. MVP 분석 규칙

### 위험 코드

| ID | 검사 대상 | 예시 |
|---|---|---|
| SEC-001 | `eval`, `exec` | 사용자 입력을 코드로 실행 |
| SEC-002 | `os.system`, 위험한 shell 호출 | 명령어 주입 가능성 |
| SEC-003 | 하드코딩된 비밀정보 | API key, password, token 의심 문자열 |
| SEC-004 | SQL 문자열 결합 | 파라미터화되지 않은 쿼리 |
| SEC-005 | 검증 없는 파일 경로 | 경로 조작 가능성 |
| SEC-006 | 민감정보 로그 출력 | 비밀번호·토큰·개인정보 노출 |

### 착한코드

| ID | 검사 대상 | 칭찬 내용 |
|---|---|---|
| GOOD-001 | 입력값 허용목록·길이 검증 | 경계에서 입력을 검증함 |
| GOOD-002 | `subprocess` 인자 배열·`shell=False` | 셸 해석을 최소화함 |
| GOOD-003 | 환경변수로 비밀정보 관리 | 비밀정보를 소스에서 분리함 |
| GOOD-004 | 파라미터화된 SQL | 사용자 입력과 쿼리를 분리함 |
| GOOD-005 | 구체적 예외 처리 | 오류 정보를 과도하게 노출하지 않음 |
| GOOD-006 | 안전한 파일 경로 처리 | 허용된 디렉터리 안에서만 파일을 다룸 |

## 6. 결과 화면

- 전체 상태: `좋음 / 주의 / 위험`
- 착한코드 배지와 잘한 이유
- 위험 finding 목록
- 심각도: `critical / high / medium / low / info`
- 파일 내 줄 번호와 짧은 코드 근거
- 왜 위험한지
- 어떻게 개선하는지
- 분석 범위와 한계

분석 결과가 0개여도 “보안 취약점이 없음”이라고 하지 않고, **“검사한 규칙 범위에서 발견되지 않음”**이라고 표시한다.

## 7. AI 사용 방식

AI는 탐지 엔진이 아니다. 정적 분석기가 만든 finding만 입력으로 받아 다음 작업을 한다.

- 여러 경고를 우선순위 순으로 요약
- 초보자도 이해할 수 있게 설명
- 착한코드가 좋은 이유 설명
- 안전한 수정 방향 제안

AI 응답에는 반드시 원래 finding ID와 줄 번호가 있어야 한다. AI가 새로운 취약점을 임의로 만들거나 근거 없는 보안 보장을 하면 해당 응답을 폐기하고 규칙 분석 결과만 보여준다.

## 8. 안전 경계

- 업로드한 Python 파일을 `import`, `exec`, `eval`, `subprocess` 등으로 실행하지 않는다.
- 파일을 영구 저장하거나 외부로 전송하지 않는다.
- 파일 크기, 줄 수, AST 노드 수에 제한을 둔다.
- 원본 소스 전체를 로그에 기록하지 않는다.
- 소스 안의 주석이나 문자열은 AI 지시사항이 아니라 분석 데이터로 취급한다.
- AI API가 없거나 실패해도 규칙 엔진 결과를 표시한다.

## 9. 결과 계약

모든 finding은 다음 필드를 가진다.

```json
{
  "rule_id": "SEC-001",
  "kind": "risk",
  "severity": "high",
  "confidence": 0.98,
  "line": 12,
  "evidence": "eval(user_input)",
  "title": "신뢰하지 않은 입력을 코드로 실행할 수 있음",
  "recommendation": "허용목록 검증 또는 안전한 파서를 사용",
  "source": "static_rule"
}
```

`rule_id`는 등록된 규칙이어야 하고, `confidence`는 0~1 사이이며, `line`은 실제 파일의 줄 번호여야 한다.

## 10. 하네스 엔지니어링 적용

이 저장소의 세 문서가 AI 에이전트의 하네스다.

- `PRD.md`: 무엇을 만들고 무엇을 만들지 않는지 정의
- `AGENTS.md`: 에이전트가 어떻게 작업하고 검증해야 하는지 정의
- `plan.md`: 누가 언제 어떤 순서로 구현하는지 정의

사람은 목표·보안 경계·수용 기준을 정하고, AI는 작은 작업을 구현한다. 테스트와 평가 fixture가 AI 결과를 검증하며, 반복되는 실패는 문서나 자동 규칙으로 승격한다.

## 11. 성공 기준

- 안전한 샘플에서 착한코드 배지와 근거가 표시된다.
- 취약한 샘플에서 위험 규칙, 줄 번호, 개선안이 표시된다.
- 같은 파일을 반복 검사하면 규칙 결과가 동일하다.
- AI 없이도 기본 리포트가 나온다.
- 업로드 파일이 실행되지 않음을 테스트로 증명한다.
- 알려진 오탐·미탐과 정적 분석의 한계를 표시한다.

## 13. 기능 요구사항 상세

### FR-01 파일 입력

- 사용자는 한 번에 하나의 `.py` 파일만 선택할 수 있다.
- `.py`가 아니면 분석 버튼을 비활성화하거나 즉시 “Python 파일만 업로드할 수 있습니다”라고 표시한다.
- 파일 크기는 MVP에서 500KB 이하로 제한한다.
- 빈 파일은 오류가 아니라 “분석할 코드가 없습니다”라는 결과를 보여준다.
- UTF-8로 읽을 수 없는 파일은 분석하지 않고 인코딩 오류를 표시한다.
- 원본 파일명은 화면 표시용으로만 사용하고 경로 정보는 제거한다.

### FR-02 분석 시작

- 사용자가 분석 버튼을 누르면 상태가 `검사 준비 → 정적 분석 중 → 결과 생성 중 → 완료` 순서로 바뀐다.
- 분석 중에는 같은 파일을 중복 제출할 수 없다.
- Python 문법 오류가 있으면 오류 위치를 줄 번호와 함께 보여준다.
- 분석기는 파일을 실행하지 않고 AST만 생성한다.
- 분석 실패 시 내부 stack trace 대신 사용자가 이해할 수 있는 메시지를 보여준다.

### FR-03 결과 요약

- 결과 상단에 전체 상태를 표시한다.
- 상태는 위험 finding이 있으면 `위험`, 중간 이하 주의 finding만 있으면 `주의`, 위험 finding이 없고 착한코드 증거가 있으면 `좋음`으로 표시한다.
- 상태는 보안 인증 점수가 아니라 발견된 증거를 요약한 값이다.
- 요약에는 `위험 N개 / 착한코드 N개 / 추가 검토 필요 N개`를 표시한다.
- “검사한 규칙 범위 밖의 문제는 찾지 못할 수 있습니다”를 항상 표시한다.

### FR-04 finding 상세

각 finding에는 다음을 표시한다.

1. 배지: 위험 또는 착한코드
2. 규칙 ID와 규칙 이름
3. 심각도
4. 신뢰도
5. 줄 번호
6. 해당 줄의 짧은 evidence
7. 왜 중요한지
8. 어떻게 개선할지
9. AI 설명인지 정적 규칙 결과인지

원본 전체 코드를 화면에 복사하지 않는다. 필요한 경우 문제 줄 전후 최대 3줄만 보여준다.

### FR-05 재검사

- 사용자는 수정한 `.py` 파일을 다시 선택할 수 있다.
- 이전 결과와 새 결과를 비교해 위험 finding 수와 착한코드 finding 수의 변화를 보여준다.
- 같은 rule ID가 해결되면 “해결됨”으로 표시할 수 있다.
- 코드가 좋아졌다는 표현은 finding 감소라는 관찰에 한정한다.

## 14. 규칙 정의의 고정 형식

모든 규칙은 다음 7개 항목을 문서와 코드에 함께 가진다.

| 항목 | 정의 |
|---|---|
| rule_id | 예: `SEC-001` |
| 목적 | 무엇을 보호하는가 |
| 탐지 조건 | 어떤 AST/텍스트 패턴을 찾는가 |
| 비탐지 조건 | 어떤 경우에는 경고하지 않는가 |
| severity | critical/high/medium/low/info |
| evidence | 사용자에게 보여줄 최소 코드 조각 |
| recommendation | 실행 가능한 개선 방향 |

### SEC-001 eval/exec

- 탐지: `eval()` 또는 `exec()` 호출이 AST에 존재한다.
- 심각도: high.
- 칭찬하지 않음: 입력값 검증만으로 호출 자체를 안전하다고 판단하지 않는다.
- 개선: 허용된 값의 매핑 테이블 또는 전용 파서를 사용한다.

### SEC-002 shell command

- 탐지: `os.system`, `os.popen`, `subprocess`의 shell 실행, 문자열 하나로 구성된 명령 호출.
- 심각도: high.
- 착한 예외: `subprocess.run([...], shell=False, check=True)`는 GOOD-002 후보.
- 개선: 인자 배열, `shell=False`, 허용목록, 최소 권한을 사용한다.

### SEC-003 hardcoded secret

- 탐지: 변수명에 `key`, `token`, `secret`, `password` 등이 포함되고 긴 문자열 리터럴이 대입된다.
- 심각도: high 또는 medium.
- 비탐지: 명백한 placeholder, 테스트용 가짜 값, 환경변수 참조.
- 한계: 정규식만으로 실제 비밀인지 확정하지 않는다.

### SEC-004 SQL concatenation

- 탐지: SQL 키워드가 포함된 문자열과 사용자 입력이 `+`, f-string, `%` 등으로 결합된다.
- 심각도: high.
- 착한 예외: placeholder와 별도 parameter tuple을 사용하는 호출.
- 개선: 파라미터화 쿼리 또는 ORM의 안전한 API를 사용한다.

### SEC-005 path traversal

- 탐지: 사용자 입력이 검증 없이 `open`, `Path`, 파일 삭제·이동 API의 경로에 전달된다.
- 심각도: high.
- 개선: 허용된 기준 디렉터리와 resolve 결과를 비교한다.

### SEC-006 sensitive logging

- 탐지: 비밀번호·토큰·authorization 값이 log/print에 전달된다.
- 심각도: medium.
- 개선: 민감값을 마스킹하고 구조화된 안전 로그를 사용한다.

### GOOD 규칙 공통 기준

착한코드는 단순히 특정 함수가 등장했다는 이유만으로 주지 않는다. 해당 코드가 보안 경계를 지키는 방향으로 사용되었는지, 줄 번호와 함께 설명할 수 있어야 한다.

## 15. 비기능 요구사항

- 재현성: 같은 파일과 같은 규칙 버전은 같은 정적 결과를 반환한다.
- 안전성: 입력 `.py`의 실행 횟수는 0회다.
- 격리성: 파일 처리는 요청 범위에서 끝나며 영구 저장하지 않는다.
- 설명가능성: 모든 위험 finding은 rule ID와 줄 번호를 가진다.
- 회복성: AI 없이도 핵심 기능이 작동한다.
- 접근성: 색상만으로 위험도를 구분하지 않고 텍스트 라벨도 표시한다.
- 성능: 500KB 이하 파일은 일반적인 노트북에서 3초 안에 정적 결과를 만든다.

## 16. 최종 데모에 반드시 포함할 파일

- 안전 예시: 환경변수, 파라미터화 SQL, `shell=False`, 입력 허용목록
- 위험 예시: `eval`, shell command, SQL 문자열 결합, 하드코딩 secret
- 경계 예시: 주석 속 `eval`, placeholder secret, 문법 오류 파일

## 17. 착한코드 판정 기준

착한코드는 특정 함수 하나를 사용했는지가 아니라, **신뢰할 수 없는 데이터가 시스템 경계를 통과할 때 검증·제한·기록·실패 처리가 설계되어 있는지**를 기준으로 판단한다. 각 기준은 `탐지 근거`, `좋은 예`, `주의할 예`, `사용자 설명`을 함께 가진다.

### 17.1 입력 검증과 데이터 경계

좋은 코드는 외부 입력을 내부 로직에 바로 전달하지 않는다.

- 입력의 타입, 길이, 형식, 허용 범위를 확인한다.
- 허용목록을 사용할 수 있으면 차단목록보다 허용목록을 우선한다.
- 검증은 데이터가 시스템에 들어오는 경계에서 수행한다.
- 검증 후에도 문자열을 명령어·SQL·파일 경로로 사용할 때 목적별 안전 처리를 다시 한다.
- 검증 실패 시 안전한 기본값이나 명확한 오류를 반환한다.

착한코드 증거 예시:

```python
ALLOWED_ROLES = {"viewer", "editor"}
if role not in ALLOWED_ROLES:
    raise ValueError("unsupported role")
```

나쁜 예시는 단순히 `if value:`만 확인하거나, 입력을 정규식으로 일부 검사한 뒤 다른 위험한 용도로 재사용하는 경우다.

### 17.2 인증과 권한 분리

- 로그인 여부와 해당 작업을 수행할 권한을 별도로 확인한다.
- 사용자 ID를 요청값만으로 신뢰하지 않는다.
- 관리자 기능은 역할·권한 검사를 명시적으로 거친다.
- 기본 권한은 최소 권한으로 설정한다.
- 권한 실패와 시스템 오류를 구분하되 민감정보는 노출하지 않는다.

착한코드 후보는 `is_authenticated` 확인 뒤에 다시 리소스 소유권 또는 역할을 확인하는 구조다. 로그인 확인만 있고 리소스 권한 확인이 없으면 착한코드로 칭찬하지 않는다.

### 17.3 비밀정보 관리

- API 키, 비밀번호, 토큰, 개인 키를 소스 코드에 넣지 않는다.
- 환경변수나 비밀 저장소에서 가져온다.
- 로그, 오류 메시지, 화면에 비밀정보를 출력하지 않는다.
- 비밀정보가 없을 때 안전하게 실패한다.
- 테스트에서도 실제 비밀정보 대신 명시적인 가짜 placeholder를 사용한다.

환경변수 사용은 GOOD-003의 근거가 되지만, 환경변수를 무조건 안전하다고 보장하지는 않는다. 접근 권한과 운영 환경 설정은 정적 분석 범위 밖임을 표시한다.

### 17.4 명령어 실행

- 셸을 거치지 않는 API를 우선한다.
- 명령과 인자를 하나의 문자열로 합치지 않고 배열로 전달한다.
- 가능하면 `shell=False`를 명시한다.
- 사용자 입력이 명령어 자체가 아니라 허용된 인자 중 하나인지 검증한다.
- 실행 결과·권한·timeout·오류 처리를 정의한다.
- 운영체제 명령이 꼭 필요하지 않으면 라이브러리 API를 선택한다.

`subprocess.run(["git", "status"], shell=False, check=True)`는 좋은 증거지만, 사용자 입력을 배열에 넣었다고 자동으로 안전 판정하지 않는다. 허용 범위와 실행 권한도 확인해야 한다.

### 17.5 데이터베이스와 SQL

- 사용자 입력과 SQL 문장을 문자열 결합하지 않는다.
- 파라미터화 쿼리 또는 검증된 ORM API를 사용한다.
- 테이블명·정렬 방향처럼 parameter로 바꿀 수 없는 값은 허용목록으로 제한한다.
- DB 오류에 SQL 전체나 인증정보를 포함하지 않는다.
- 읽기·쓰기 권한을 목적에 맞게 분리한다.

좋은 예시는 SQL 템플릿과 parameter tuple이 분리된 구조다. 단순히 SQL 문자열이 함수에 전달된다는 이유만으로 GOOD-004를 부여하지 않는다.

### 17.6 파일·경로 처리

- 사용자 입력을 파일 경로로 사용할 때 기준 디렉터리를 정한다.
- `resolve()`한 결과가 허용 디렉터리 안에 있는지 확인한다.
- `..`, 절대 경로, 심볼릭 링크 등 우회 가능성을 고려한다.
- 파일을 열 때 필요한 모드와 인코딩을 명시한다.
- 임시 파일은 예측하기 어려운 이름과 제한된 권한을 사용한다.
- 파일 오류 시 경로·서버 구조·민감정보를 그대로 노출하지 않는다.

허용 디렉터리 확인이 코드에 드러나고, 파일 접근 실패가 안전하게 처리되면 GOOD-006 후보가 된다.

### 17.7 오류 처리와 로그

- 예외를 모두 삼키는 `except Exception: pass`를 사용하지 않는다.
- 예상 가능한 오류와 예상하지 못한 오류를 구분한다.
- 사용자에게는 안전한 메시지를, 내부 로그에는 추적 가능한 이벤트를 남긴다.
- 로그에 비밀번호, 토큰, 세션 값, 전체 요청 본문을 기록하지 않는다.
- 오류 메시지에 stack trace, 파일 경로, SQL, 내부 키를 그대로 노출하지 않는다.
- 로그 수준과 이벤트 의미가 일관적이다.

구체적인 예외를 처리하고 민감정보를 제거한 로그를 남기는 경우 GOOD-005 후보가 된다.

### 17.8 직렬화·외부 데이터 처리

- 신뢰할 수 없는 데이터를 위험한 역직렬화 API로 처리하지 않는다.
- JSON 등 데이터 전용 형식을 우선한다.
- 외부 응답의 타입과 필수 필드를 확인한다.
- 예상하지 못한 필드나 과도하게 큰 응답을 제한한다.
- 외부 데이터가 내부 명령·HTML·SQL로 재사용될 때 목적별 escaping/parameterization을 적용한다.

### 17.9 암호화와 개인정보

- 직접 암호 알고리즘을 구현하지 않고 검증된 라이브러리를 사용한다.
- 비밀번호를 평문 저장하지 않고 적절한 password hashing을 사용한다.
- 암호화 키와 암호문을 같은 위치에 하드코딩하지 않는다.
- 개인정보는 필요한 만큼만 수집·보관·로그 처리한다.
- 복호화 키가 코드와 같은 곳에 있으면 착한코드로 인정하지 않는다.

### 17.10 코드 구조와 유지보수성

보안 좋은 코드는 보안 의도가 코드 구조에서 읽힌다.

- 입력 검증, 권한 확인, 핵심 처리, 출력 변환을 분리한다.
- 보안 경계를 담당하는 함수 이름이 모호하지 않다.
- 위험한 동작을 여러 곳에서 직접 호출하지 않고 안전한 wrapper로 제한한다.
- 실패 시 기본값이 더 안전한 방향으로 동작한다.
- 보안 규칙을 우회하는 전역 상태나 숨겨진 부작용을 줄인다.
- 주석은 “무엇을 한다”보다 “왜 이 보안 제약이 필요한가”를 설명한다.

## 18. 착한코드 등급

| 등급 | 의미 | 부여 조건 |
|---|---|---|
| Good Evidence | 좋은 보안 습관의 증거 | 한 가지 보안 기준을 코드 위치와 함께 확인 |
| Strong Good | 여러 방어가 연결됨 | 검증·권한·오류 처리 등 두 가지 이상이 함께 확인 |
| Defensive Design | 보안 경계가 구조적으로 설계됨 | 위험 동작이 제한된 wrapper와 테스트 가능한 경계 안에 있음 |

등급은 전체 애플리케이션의 안전 등급이 아니다. 분석된 코드 조각에서 관찰된 방어 증거의 등급이다.

## 19. 판정 아키텍처 확정

제품 실행 시 AI는 사용하지 않는다. 보안 판정의 단일 기준은 사람이 검토한 규칙 DB다.

```text
.py 입력
  ↓
입력 검증
  ↓
Python AST 생성
  ↓
규칙 DB의 rule pattern과 비교
  ↓
rule_id로 설명·심각도·개선안 조회
  ↓
finding과 착한코드 배지 생성
  ↓
고정된 결과 화면
```

규칙 DB는 최소한 다음 필드를 가진다.

```json
{
  "rule_id": "GOOD-002",
  "category": "command_execution",
  "kind": "good_practice",
  "pattern": "subprocess_args_and_shell_false",
  "severity": "info",
  "confidence": 0.95,
  "title": "명령 실행 경계를 제한함",
  "why_it_matters": "셸 해석과 인자 혼합 위험을 줄임",
  "recommendation": "허용된 명령과 인자만 사용하고 실행 권한을 최소화",
  "version": "1.0"
}
```

AI는 개발 단계에서 문서·테스트·코드 리뷰 초안을 만드는 데만 사용할 수 있다. AI가 제안한 규칙은 팀원 A의 승인, 팀원 C의 DB 등록, 팀원 B의 fixture 테스트를 통과하기 전에는 제품 판정에 사용하지 않는다.
