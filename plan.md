# plan.md — 착한코드검거단 역할 계획

## 공통 구현 방향

제품 실행 중 보안 판정에는 AI를 사용하지 않는다. 사용자가 `.py` 파일을 넣으면 정적 분석기가 코드를 실행하지 않고, 사람이 검토한 규칙 DB와 비교하여 결과를 만든다.

`파일 입력 → AST 분석 → 규칙 DB 조회 → finding 생성 → 고정 설명 표시 → 재검사`

AI는 개발 과정에서만 문서 초안, 테스트 초안, 코드 리뷰 보조로 사용할 수 있다. AI가 위험 여부, 심각도, 착한코드 배지를 결정하지 않는다.

## 팀원 A — 제품·보안 기준 담당

### 책임

- 사용자가 어떤 코드를 좋은 보안 코드로 이해해야 하는지 정의한다.
- 규칙 DB에 들어갈 보안 기준을 승인한다.
- 각 규칙의 탐지 조건, 비탐지 조건, 심각도, 신뢰도, 설명, 개선안을 결정한다.
- 입력 검증, 권한, 비밀정보, 명령 실행, SQL, 파일 경로, 오류·로그 기준을 검토한다.
- 위험 결과와 착한코드 결과가 충돌할 때 우선순위를 정한다.
- “안전 보장”, “취약점 없음”처럼 과장된 표현을 제거한다.
- 위험·안전·경계 fixture의 기대 결과를 승인한다.
- 최종 결과 화면과 데모가 제품 목표에 맞는지 확인한다.

### 산출물

- 규칙 정의서가 포함된 `PRD.md`
- 규칙별 판정 기준과 사용자 표시 문구
- fixture 정답표
- 최종 수용 기준
- 데모 발표 시나리오

### 완료 기준

- 모든 규칙에 탐지/비탐지 조건이 있다.
- 좋은 코드의 근거가 특정 줄과 연결된다.
- 규칙 DB 변경을 승인하거나 반려할 수 있다.

## 팀원 B — Python 정적 분석 엔진 담당

### 책임

- `.py` 확장자, 파일 크기, 인코딩, 빈 파일을 검증한다.
- 파일을 import, compile, exec, eval, subprocess로 실행하지 않는다.
- Python AST를 생성하고 문법 오류 위치를 반환한다.
- 규칙 DB의 pattern을 읽어 AST 노드와 호출을 검사한다.
- 위험 규칙과 착한코드 규칙의 매칭 결과를 생성한다.
- 실제 줄 번호, column, 짧은 evidence를 추출한다.
- 규칙 DB에 존재하지 않는 rule ID는 결과로 만들지 않는다.
- 같은 입력과 같은 규칙 DB 버전에서 동일한 결과를 반환한다.

### 분석 대상 규칙

- `SEC-001` eval/exec
- `SEC-002` shell 명령 실행
- `SEC-003` 하드코딩 비밀정보
- `SEC-004` SQL 문자열 결합
- `SEC-005` 검증되지 않은 파일 경로
- `SEC-006` 민감정보 로그
- `GOOD-001` 입력 허용목록/길이 검증
- `GOOD-002` 안전한 subprocess 사용
- `GOOD-003` 환경변수 비밀정보
- `GOOD-004` 파라미터화 SQL
- `GOOD-005` 구체적인 예외 처리
- `GOOD-006` 안전한 경로 처리

### 산출물

- 입력 검증기
- AST 분석기
- RuleRegistry
- 규칙 DB 로더
- finding 생성기
- positive/negative/boundary fixture
- 단위·회귀 테스트

### 완료 기준

- 위험 샘플과 안전 샘플을 구분한다.
- 모든 finding에 rule ID와 실제 줄 번호가 있다.
- 분석 과정에서 입력 파일이 실행되지 않음을 테스트로 증명한다.
- 규칙 DB가 잘못되면 조용히 오판하지 않고 오류를 반환한다.

## 팀원 C — 규칙 DB·통합 품질 담당

### 책임

- 보안 판정을 담당하는 규칙 DB 구조를 만든다.
- 각 레코드에 `rule_id`, `category`, `kind`, `pattern`, `severity`, `confidence`, `title`, `why_it_matters`, `recommendation`, `version`을 둔다.
- positive/negative/boundary fixture와 규칙 ID의 연결을 관리한다.
- rule ID 중복, 필수 필드 누락, 잘못된 severity, 잘못된 pattern을 검사한다.
- 규칙 DB 버전을 결과에 포함시킨다.
- 규칙 변경 시 어떤 결과가 달라졌는지 기록한다.
- 네 명이 공통으로 사용할 규칙 DB schema와 변경 절차를 관리한다.
- 각 담당자가 자신의 영역에서 AI 에이전트로 만든 결과가 공통 schema를 지키는지 확인한다.
- 통합 전 규칙 DB·엔진·UI의 연결 상태를 검사한다.
- 반복되는 실패를 문서나 자동 검사로 승격한다.

### 산출물

- 규칙 DB schema
- 초기 규칙 레코드 12개
- 규칙 DB 검증기
- 규칙 버전 및 변경 이력
- 작업 카드 형식
- 에이전트 검증 루프
- 규칙 DB 회귀 테스트

### 완료 기준

- 외부 AI·네트워크 없이도 판정 결과가 나온다.
- 같은 rule ID는 항상 같은 의미와 설명을 가진다.
- 규칙 DB와 코드·fixture가 불일치하면 테스트가 실패한다.
- AI가 만든 초안은 해당 담당자의 검토와 팀 합의 전까지 제품 규칙이 되지 않는다.

## 팀원 D — UI·통합·실행 담당

### 책임

- 사용자가 `.py` 파일을 선택하고 검사하는 화면을 만든다.
- 분석 중, 완료, 문법 오류, 입력 오류, 규칙 DB 오류 상태를 표시한다.
- `좋음 / 주의 / 위험` 신호등을 구현한다.
- 착한코드 배지, 규칙 ID, 심각도, 줄 번호, evidence, 고정 설명, 개선안을 표시한다.
- 정적 규칙 결과와 사용자 해석 문구를 연결한다.
- 원본 전체를 공개하지 않고 필요한 줄 주변만 보여준다.
- 수정된 파일을 다시 검사하고 결과 변화를 보여준다.
- 엔진·규칙 DB·화면을 통합한다.
- 외부 AI 없이 로컬에서 실행되는 데모 환경을 준비한다.

### 산출물

- 파일 입력 화면
- 결과 리포트 화면
- 착한코드 배지 UI
- 오류 및 재검사 흐름
- 통합 테스트
- 실행 방법과 최종 데모

### 완료 기준

- 사용자가 30초 안에 좋은 점과 위험 점을 찾을 수 있다.
- 모든 결과에 줄 번호와 근거가 보인다.
- 규칙 DB의 고정 설명이 그대로 연결된다.
- 네트워크나 AI API가 없어도 데모가 동작한다.

## 네 명이 함께 사용하는 하네스 운영 방식

하네스는 팀원 C의 전담 업무가 아니다. 네 명 모두 아래 사이클을 자신의 담당 영역에서 독립적으로 수행한다.

`목표를 작업 카드로 분해 → 관련 문서·범위·완료 조건을 AI 에이전트에게 제공 → AI 구현 → 자동 테스트 → 사람 리뷰 → 실패 원인을 문서/규칙/테스트에 반영 → 통합 결과 공유`

- A는 보안 규칙 초안을 AI로 정리한 뒤 사람이 기준을 승인한다.
- B는 AST 분석을 AI로 구현한 뒤 실행 금지 테스트와 fixture로 검증한다.
- C는 규칙 DB·통합 코드를 AI로 구현한 뒤 schema와 연결 테스트로 검증한다.
- D는 UI·실행 환경을 AI로 구현한 뒤 샘플·오류 입력·clean run으로 검증한다.

각자의 하네스 결과는 공통 PRD, finding schema, rule ID, 테스트를 통해 하나의 제품으로 합쳐진다.

## 담당자 간 인계 규칙

1. 담당자가 작업 카드와 완료 조건을 작성한다.
2. 담당자가 AI 에이전트를 사용해 초안과 테스트를 만든다.
3. 담당자가 직접 검토하고 자신의 완료 조건을 통과시킨다.
4. A가 규칙 의미와 보안 표현을 승인한다.
5. B가 탐지 결과와 fixture를 연결한다.
6. C가 규칙 DB·schema·버전을 통합한다.
7. D가 finding을 화면과 데모에 연결한다.
8. 네 명이 최종 통합 결과를 함께 검증한다.

규칙 하나를 변경할 때는 이 다섯 단계를 모두 거친다. 규칙 DB만 변경하거나 화면 문구만 바꾸는 식으로 일부만 변경하지 않는다.

## 전체 일정

| 날짜 | 팀 일정 | 팀원 A | 팀원 B | 팀원 C | 팀원 D |
|---|---|---|---|---|---|
| 8일 | 하네스용 Markdown 파일 확정 | `PRD.md`의 제품·보안 기준 확인 | `AGENTS.md`의 정적 분석 작업 규칙 확인 | 규칙 DB schema와 작업 카드 확인 | `plan.md`의 UI·통합 범위와 일정 확인 |
| 9일 | 개발, 밤 12시까지 본인 개발 현황 공유 | 위험·착한코드 규칙과 fixture 기대 결과 확정 | 파일 입력 검증과 AST 분석기 개발 | 규칙 DB schema와 초기 규칙 레코드 개발 | `.py` 파일 입력과 분석 상태 화면 개발 |
| 10일 | 개발, 밤 12시까지 본인 개발 현황 공유 | 판정 문구와 오탐·미탐 검토 | 위험·착한코드 규칙 매칭과 finding 생성 개발 | 규칙 DB 검증기와 회귀 테스트 개발 | 결과 리포트와 착한코드 배지 UI 개발 |
| 11일 | 개발, 밤 12시까지 본인 개발 현황 공유 | 최종 수용 기준과 데모 결과 검토 | fixture·실행 금지·반복 실행 테스트 | 엔진·규칙 DB·schema 통합 검증 | 재검사 흐름 통합과 로컬 데모 실행 점검 |
| 12일 | 데모 영상 제작 | 데모 시나리오와 표현 확인 | 안전·위험·경계 예시 분석 시연 | 규칙 DB 결과와 테스트 결과 확인 | 화면 시연과 데모 영상 제작 |
| 13일 | 발표 준비 | 제품 목표와 보안 기준 발표 준비 | 정적 분석 엔진 발표 준비 | 하네스·규칙 DB·검증 방식 발표 준비 | UI·통합·데모 발표 준비 |
| 14일 | 발표 | 제품·보안 기준 발표 | 분석 엔진 발표 | 하네스·검증 방식 발표 | UI·데모 발표 |

8일부터 11일까지를 4일 작업 기간으로 운영한다. 9일, 10일, 11일에는 각 팀원이 밤 12시까지 본인의 개발 현황을 공유한다.

## 4일 집중 구현 범위

4일 동안 완성도를 확보할 핵심 범위는 단일 `.py` 파일, 12개 내외의 명확한 규칙, 규칙 DB 비교, 로컬 실행, fixture 기반 테스트, 결과 화면이다. 각 담당자는 새로운 기능을 계속 추가하기보다 이 흐름이 처음부터 끝까지 재현되도록 책임진다.
